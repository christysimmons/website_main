(function (){

	var title_options = [
		"decently reliable human organism",
		"a human who pokes at keyboards",
		"code minigolfer",
		"tap tap tap tap tap",
		"spends a lot of time with flexbox for some reason",
		"maybe don't global-install that npm package",
		"document.getElementBySurname",
		"node minor demon. More of a node angry sprite, really."
	];

	var titleChoice = title_options[Math.floor(Math.random() * title_options.length)];
	document.title = "Christina Simmons: " + titleChoice;
})();


